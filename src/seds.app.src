{application, seds, [
    {description, "Socket over DNS service"},
    {vsn, "0.3.0"},
    {registered, [seds_srv,seds_sup]},
    {applications, [kernel, stdlib, sasl, lager]},
    {mod, { seds_app, []}},
    {env, []}
    ]}.
